# Build artifacts and fetched deps (regenerated by `mix`)
/_build/
/deps/
/.fetch
/cover/
/doc/
*.ez
*.beam
erl_crash.dump

# Secrets — never commit tokens/keys
.env
.env.*
/config/*.secret.exs

# Editor / language server
/.elixir_ls/
/.lexical/
.DS_Store
